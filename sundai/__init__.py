"""Sundai — foundation models for the physical world.

Board streams sensors over USB serial -> laptop runs the foundation model ->
decision -> command back to the board. The model never runs on the MCU.
"""
__version__ = "0.1.0"
