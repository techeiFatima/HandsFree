"""Hands-free HCI — webcam gestures driving the real machine.

Camera pixels become a word (Nicholas: sources, gestures, cursor).
That word becomes something happening (Fatima: actions, beats).
The two halves only touch at the word.
"""
__version__ = "0.1.0"
